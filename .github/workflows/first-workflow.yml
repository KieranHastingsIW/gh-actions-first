name: First Workflow 
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout@v3
      - name: setup dependencies 
        run: npm ci 
      - name: run tests
        run: npm test
        
